// import styled from 'styled-components';
import { TextContainer, Text } from '../../../../components/AlertInfoScreen/styles';

export default function NoEnrollmentScreen() {
  return (
    <TextContainer>
      <Text>Você precisa completar sua inscrição antes de prosseguir pra escolha do ingresso</Text>
    </TextContainer>
  );
}
